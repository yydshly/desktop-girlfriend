# Character Reference

`character-reference.png` 是第一版生成的统一角色参考图。

用途：

- 作为 image-to-video 的输入图，生成 `wave.webm`、`sing.webm` 等动作素材。
- 作为后续 Live2D/Spine 分层绘制的视觉参考。
- 用来约束角色一致性：发型、发夹、粉色毛衣、白色百褶裙、温柔气质。

注意：

- 当前图片是整图参考，不是透明角色层。
- 不建议直接拿它做最终桌面透明角色。
- 如果生成动作视频时脸部漂移明显，应停止视频扩展，改做分层角色资产。
